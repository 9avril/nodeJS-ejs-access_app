<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Manufacturers</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            background-color: antiquewhite;
            font-family: Arial;
        }

        h1 {
            padding-left: 10px;
        }

        table {
            font-size: large;
            padding-left: 15px;
        }

        th {
            padding-right: 10px;
        }

        td {
            padding-right: 20px;
            text-align: start;
        }
        input[type="text"] {
            border: 2px solid #ccc;
            border-radius: 4px;
            padding: 8px 12px;
            font-size: 16px;
            transition: border-color 0.3s ease;
        }

        input[type="text"]:focus {
            border-color: #007bff;
            outline: none;
        }
    </style>
</head>
<body>
<h1>Manufacturers</h1>
<form action="/manufacturers" method="get">
    <label for="maxSum">Max Price:</label>
    <input type="number" id="maxSum" name="maxSum" value="<%= maxSum %>">
    <button type="submit">Search</button>
</form>
<table>
    <thead>
    <tr>
        <th>Name</th>
    </tr>
    </thead>
    <tbody>
    <% for (let i = 0; i < manufacturers.length; i++) { %>
        <tr>
            <td><%= manufacturers[i].name %></td>
        </tr>
    <% } %>
    </tbody>
</table>
</body>
</html>
